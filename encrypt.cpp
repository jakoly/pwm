#include "encrypt.h"
#include <string>
using namespace std;

encrypt::encrypt() {}

string encryptText(string password)  {
    string encryptedText;

    encryptedText = password;



    return encryptedText;
}