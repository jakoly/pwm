#ifndef ENCRYPT_H
#define ENCRYPT_H
#include <string>
#include "stdio.h"
#include "stdlib.h"

class encrypt
{
public:
    encrypt();
};

std::string encryptText(std::string password);

#endif // ENCRYPT_H
